
class Shadow {
    offset: Point;
    blurRadius: number;
    color: Color;

    constructor(offset: Point, blurRadius: number, color: Color) {
        this.offset = offset;
        this.blurRadius = blurRadius;
        this.color = color;
    }
}
